package controller;

/**
 *Enum которые определяет результат хода
 * NONE значит нельзя так походить
 * NORMAL значит ход обычный
 * KILL значит шашка съела другую
 */
public enum MoveType {
    NONE, NORMAL, KILL
}
